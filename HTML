<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Birthday Surprise for FAA</title>
    <style>
        body { background: #0f172a; color: #fff; font-family: 'Segoe UI', sans-serif; display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: 100vh; margin: 0; }
        .content { width: 90%; max-width: 500px; text-align: center; }
        .box { background: #1e293b; padding: 30px; border-radius: 20px; box-shadow: 0 4px 20px rgba(0,0,0,0.3); margin-bottom: 20px; }
        .nav-buttons { display: flex; justify-content: space-between; margin-top: 30px; }
        button { background: #334155; color: white; border: none; padding: 10px 20px; border-radius: 50px; cursor: pointer; transition: 0.3s; }
        button.next { background: #f472b6; }
        button:hover { opacity: 0.8; }
        .hidden { display: none; }
    </style>
</head>
<body>

    <!-- Step 1: Countdown -->
    <div class="content" id="view1">
        <div class="box">
            <h1>ALMOST THERE!</h1>
            <div id="countdown" style="font-size: 2.5rem; font-weight: bold;">00 : 09 : 07 : 24</div>
        </div>
        <div class="nav-buttons">
            <span></span>
            <button class="next" onclick="show('view2')">Next ></button>
        </div>
    </div>

    <!-- Step 2: Mesej Utama -->
    <div class="content hidden" id="view2">
        <div class="box">
            <h2>Happy Birthday, FAA!</h2>
            <p>Turning 17!</p>
        </div>
        <div class="nav-buttons">
            <button onclick="show('view1')">< Back</button>
            <button class="next" onclick="show('view3')">Next ></button>
        </div>
    </div>

    <!-- Step 3: Unlock -->
    <div class="content hidden" id="view3">
        <div class="box">
            <div style="font-size: 3rem;">🔒</div>
            <p>Complete the steps to unlock the content</p>
            <button class="next" onclick="unlock()">Unlock my gift ></button>
        </div>
    </div>

    <!-- Fail Lagu -->
    <audio id="bgMusic" loop>
        <source src="Until I Found You.mp3" type="audio/mp3">
    </audio>

    <script>
        function show(viewId) {
            document.querySelectorAll('.content').forEach(el => el.classList.add('hidden'));
            document.getElementById(viewId).classList.remove('hidden');
            // Muzik start bila user tekan butang
            document.getElementById("bgMusic").play().catch(() => {});
        }
        function unlock() { alert("Surprise! I love you!"); }
    </script>
</body>
</html>
